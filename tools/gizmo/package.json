{
  "name": "lambda-gizmo",
  "version": "0.1.0",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "build": "node ./node_modules/typescript/bin/tsc -p tsconfig.json",
    "test": "npm run build --silent && node dist/test/smoke.js",
    "gizmo": "npm run build --silent && node dist/src/cli.js"
  },
  "devDependencies": {
    "typescript": "^5.8.0"
  }
}

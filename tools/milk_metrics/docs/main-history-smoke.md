# Main-history smoke patch

This note is a minimal image-metrics gadget change used to verify the targeted JSON button, gadget landing, gadget promotion, and main-history receipt path.

It is intentionally documentation-only so the quick image-metrics verification profile remains focused on Python package compilation.

# Image metrics gadget development

This file lives on the `metrics/image-metrics` gadget branch when landed through the gadget JSON workflow.

The purpose of this note is to test that a patch can be routed to:

```text
gadgets/metrics/image-metrics/main
```

and then synchronized to:

```text
gadget-agents/metrics/image-metrics/ed
gadget-agents/metrics/image-metrics/edd
gadget-agents/metrics/image-metrics/eddy
gadget-agents/metrics/image-metrics/guy
```

This does not promote the change to repository `main`.

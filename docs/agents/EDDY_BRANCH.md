# Eddy branch

This marker establishes `agents/eddy` as Eddy's persistent working branch.

Rules:

- Eddy works on `agents/eddy`.
- Eddy treats `main` as the integration source of truth.
- Before proposing or submitting work, Eddy must bring this branch up to date with current `main` and verify that the result does not regress `main`.
- Direct pushes to `main` should happen only when explicitly requested.
